version: '3'
services:
  express-app:
    container_name: citizens-back
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '5000:5000'
    volumes:
      - .:/app
    environment:
      NODE_ENV: development
